# 更新日志

## v0.4.3（2026-09-02）

- feat: `references/ai-flavor-guide.md` 新增「〇、使用原则」——反指纹原则（每篇只选 3-5 种手法，全用本身就是新指纹；依据 StoryScope 检测研究：表层风格改动对检测率影响极小，结构层与经验层才是主要特征）；四层 AI 味模型标注处理顺序（自下而上：经验层→结构层→句式层→词汇层）
- feat: `gin-wechat-article-polish/SKILL.md` 动作 5 对齐使用原则（3-5 种手法预算 + 自下而上顺序 + 六种诊断复检"清理后发扁"）；边界新增两条——去 AI 腔只动表达层；表达层救不回来时返回 core「需重写」信号触发 writer 重写，禁止在烂底子上反复润色
- docs: 灵感来源评估：sepia（Nanako0129/sepia）方法论的可用增量仅限"校准原则 + 处理顺序纪律"两点，小说 rubric 与英文 venue 规则不适用于公众号长文，未引入

## v0.4.2（2026-08-30）

- fix: 修复 stage_validator 校验逻辑未被主 skill 调用的问题
- feat: 新增 `scripts/flow_controller.py`，作为推进 stage 的唯一强制入口
- feat: 新增 `scripts/sub_skill_guard.py`，检查子 skill 是否真正执行过
- feat: `stage_validator.py` 增加子 skill 执行证据校验（context key + 输出文件时间戳）
- feat: `progress_reporter.py` 增加跳过步骤警告和阻塞提示渲染
- feat: `gin-wechat-article-angle/SKILL.md` 和 `gin-wechat-article-outline/SKILL.md` 增加被跳过检测
- docs: 更新 `gin-wechat-article-core/SKILL.md`，明确每次推进 stage 前必须调用 `flow_controller.py`

## v0.4.1（2026-08-30）

- style: Progress checklist 已完成标记从 `[x]` 统一改为 `[✓]`，与设计指南保持一致
- fix: 更新 `scripts/progress_reporter.py` 和 `tests/test_progress_reporter.py` 中的完成标记
- docs: 更新 `gin-wechat-article-core/SKILL.md` 进度条规则中的标记说明

## v0.4.0（2026-08-28）

- feat: 为公众号长文写作主编排流程增加 Progress checklist（6 阶段宏观仪表盘 + 子 skill 微观 checklist）
- feat: 新增 `scripts/progress_reporter.py` 统一渲染 checklist
- feat: 7 个子 skill SKILL.md 增加 Progress checklist 展示规则
- docs: `gin-wechat-article-core/SKILL.md` 增加进度条使用规则、6 阶段映射、阶段标签

## v0.3.5 及之前

- 详见 git 提交历史
